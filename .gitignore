# secrets
.env

# data
data/raw/
!data/raw/.gitkeep

# caches
__pycache__/
*.pyc
.cache/
demo/.llm_cache/
.venv/
.ipynb_checkpoints/

# os
desktop.ini
.DS_Store


# trained weights (regenerate with: python -m sentr.train_classifier)
models/

# colab upload bundle (rebuild with: python notebooks/pack_for_colab.py)
notebooks/sentr_colab.zip

# transient demo output -- rewritten on every run, not a committed claim
demo/.runs/

# default audit sink -- written by any pipeline call that omits log_path
eval/results/audit_log.jsonl

# transient verification output -- re-derived on every verify_all.py run,
# never a committed claim
eval/results/.verify/
eval/results/verify_baseline.json
eval/results/day4_sentr.json
